from django.contrib import admin

from .models import Turma  # Coloca o model para ser registrado através da interface administrativa
# Register your models here.
admin.site.register(Turma) # Coloca o model para ser registrado através da interface administrativa
